<h1>All contributors</h1>

<h2><%= link_to "Create a new contributor profile", profile_path(:new) %></h2>

<ul>
  <% @profiles.each do |profile| %>
  <li><%= link_to profile.name, profile_path(profile) %>,  (<%= profile.age %>), <%= profile.occupation %></li>
  <% end %>
</ul>
